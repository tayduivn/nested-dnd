import React from "react";
import { Row, Col } from "react-bootstrap";

export default class CharacterSettings extends React.Component {
	render() {
		return (
			<Row className="status-bar alert alert-default">
				<Col xs={12}>Settings</Col>
			</Row>
		);
	}
}
